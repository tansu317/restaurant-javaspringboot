server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/restaurantdb
    username: restaurant_user
    password: restaurant321
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        enable_lazy_load_no_trans: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  profiles:
    active: production